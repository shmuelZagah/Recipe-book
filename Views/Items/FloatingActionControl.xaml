<?xml version="1.0" encoding="utf-8" ?>
<ContentView xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Recipe_book.Views.Items.FloatingActionControl"
             x:Name="Root">

    <Border BackgroundColor="{Binding ButtonColor, Source={x:Reference Root}}"
            WidthRequest="{Binding ButtonSize, Source={x:Reference Root}}"
            MinimumHeightRequest="{Binding ButtonSize, Source={x:Reference Root}}"
            StrokeThickness="0"
            StrokeShape="RoundRectangle 18"
            HorizontalOptions="Center"
            VerticalOptions="Center">

        <Border.Shadow>
            <Shadow Brush="Black" Offset="0,6" Opacity="0.25" Radius="8" />
        </Border.Shadow>

        <Border.GestureRecognizers>
            <TapGestureRecognizer Command="{Binding Command, Source={x:Reference Root}}"
                                  CommandParameter="{Binding CommandParameter, Source={x:Reference Root}}"/>
        </Border.GestureRecognizers>

        <VerticalStackLayout HorizontalOptions="Center" VerticalOptions="Center" Spacing="2" Padding="5">

            <Image Source="{Binding IconSource, Source={x:Reference Root}}"
                   WidthRequest="{Binding IconSize, Source={x:Reference Root}}"
                   HeightRequest="{Binding IconSize, Source={x:Reference Root}}"
                   HorizontalOptions="Center" />

            <Label Text="{Binding Text, Source={x:Reference Root}}"
                   TextColor="{Binding TextColor, Source={x:Reference Root}}"
                   FontSize="12"
                   FontAttributes="Bold"
                   HorizontalOptions="Center"
                   HorizontalTextAlignment="Center"
                   IsVisible="{Binding HasText, Source={x:Reference Root}}" />

        </VerticalStackLayout>
    </Border>
</ContentView>